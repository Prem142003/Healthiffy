export const SUBSCRIPTION_STATUS = Object.freeze({
  ACTIVE: 'ACTIVE',
  EXPIRED: 'EXPIRED',
  CANCELLED: 'CANCELLED'
});

export const DELIVERY_STATUS = Object.freeze({
  DELIVERED: 'DELIVERED'
});

